name: Docker Image CI

on:
  push:
    branches: [ "docker-workflows" ]
  pull_request:
    branches: [ "docker-workflows" ]

jobs:

  build: 

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: login to docker hub
      run: echo "dckr_pat_y5kC4-NGNpLCzeP3itAGJU8LFpk" | docker login -u "dnicoswirl" --password-stdin
    - name: Build the Docker image   
      run: docker buildx build -t dnicoswirl/test:multi-latest --platform linux/amd64,linux/arm64 --push .
